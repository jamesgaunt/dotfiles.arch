general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid spawning multiple lockers
    before_sleep_cmd = loginctl lock-session     # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on   # turn screen back on after wake
}

listener {
    timeout = 300                                 # 5 min: lock
    on-timeout = loginctl lock-session
}

listener {
    timeout = 330                                 # 5.5 min: screen off
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

# Auto-suspend disabled 2026-06-22: NVIDIA + Hyprland resume bug leaves the
# keyboard dead in hyprlock for ~20s after a real S3 resume. This is an
# always-on desktop, so we lock + blank the screen but never enter S3.
# (See setup notes / suspend-keyboard-dead investigation.)
# listener {
#     timeout = 1800                                # 30 min: suspend
#     on-timeout = systemctl suspend
# }
